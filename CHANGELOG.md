# Changelog

## [Unreleased]

### Changed

- **Breaking**: renamed the import name and PyPI package name from `notegod` to `fungod` to match the repository name. Anyone doing `import notegod` or `pip install notegod` must switch to `import fungod` / `pip install fungod`.
- The old `notegod` PyPI package will get one final release forwarding to `fungod` (manual follow-up by the repo owner).
